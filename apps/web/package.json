{
  "name": "@ride/web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run check:hooks && tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "npm run check:hooks && tsc --noEmit",
    "check:hooks": "node scripts/check-hooks.mjs"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
